<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lisa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OnionPartActivity"></activity>
        <activity android:name=".PreProductionRecordActivity" />
        <activity android:name=".InventoryForecastActivity" />
        <activity android:name=".MaterialInventoryActivity" />
        <activity android:name=".ProductionRecipeActivity" />
        <activity android:name=".OtherActivity" />
        <activity android:name=".FlavourQuantityActivity" />
        <activity android:name=".ProductionRecordActivity" />
        <activity android:name=".RemarkActivity" />
        <activity android:name=".OtherAddActivity" />
        <activity android:name=".FlavourActivity" />
        <activity android:name=".StockInOutActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>